{% extends 'base.html.twig' %}

{% block title 'Update - Pin #' ~ pin.id  %}

{% block body %}
	<h1>{{ 'Update - Pin #' ~ pin.id }}</h1>
	{{ form_start(form) }}
	{{ form_widget(form) }}
	<input type="submit" value="Update Pin #{{ pin.id }}">
	{{ form_end(form) }}
{% endblock %}
